class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        
        cuvinte=s.split()

        rez=len(cuvinte[len(cuvinte)-1])

        return rez
